@using Hotel.WebUI.Models
@model ReviewViewModel
@{
    ViewBag.Title = "Index";
}

    <h1>Отзывы</h1>
    @foreach (var it in @Model.Reviews)
    {
        @Html.Partial("ReviewItems",it)
    }

    @*    <p class="text-center"><button type="button" class="btn btn-dark" data-toggle="modal" data-target=".bd-example-modal-lg">Оставить отзыв!</button></p>*@

    @Html.ActionLink("AddFeedback", "AddFeedback", "Review")

